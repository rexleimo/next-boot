export default function Loading() {
  return (
    <div className="h-screen w-screen flex items-center justify-center">
      <div className="text-center">
        <h2 className="text-xl font-bold">Ha加载中...</h2>
        {/* 这里可以添加loading动画或logo */}
      </div>
    </div>
  );
}
